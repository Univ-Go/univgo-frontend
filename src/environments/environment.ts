export const environment = {
  production: false,
  apiBaseUrl: '/api',
  /**
   * PrimeNG 22 verifies a PrimeUI licence offline and shows a banner until one is supplied.
   * Not a secret: the key is validated client-side and ships in the bundle either way.
   */
  primeUiLicense: '',
};
